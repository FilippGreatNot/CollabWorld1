ITS_temp_gameplay_debug = {
	ITS_kill_san = {
		icon = GFX_decision_generic_form_nation

		available = {
			NOT = {country_exists = TUS}
		}
		visible = {
			always = yes
		}
		
		cost = 0
		
		days_remove = 10
		fire_only_once = yes
		
		remove_effect = {
			declare_war_on = {
				target = SAN
				type = annex_everything
			}
			929 = {add_core_of = ROOT}
			907 = {add_core_of = ROOT}
		}
		
		ai_will_do = {
			factor = 100
		}	
	}
	
	ITS_kill_ita = {
		icon = GFX_decision_generic_form_nation

		available = {
			NOT = {country_exists = VNE }
			NOT = {country_exists = SAN }
			NOT = {country_exists = TUS }
		}
		visible = {
			always = yes
		}
		
		cost = 0
		
		days_remove = 70
		fire_only_once = yes
		
		remove_effect = {
			declare_war_on = {
				target = ITA
				type = annex_everything
			}
		}
		
		ai_will_do = {
			factor = 100
		}	
	}
}