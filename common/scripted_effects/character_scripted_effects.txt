#Actually this is used for advisors only
#I don't know why
#I was just following orders
kill_character = {
	custom_effect_tooltip = kill_character_effect
	hidden_effect = {
		set_character_flag = dead
	}
}
revive_character = { #I don't think it will be used but I already used it in the debug decision so cope
	custom_effect_tooltip = revive_character_effect
	hidden_effect = {
		clr_character_flag = dead
	}
}