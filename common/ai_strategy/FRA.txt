# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence

triple_entente_fra = {
	allowed = { original_tag = FRA }

	enable = {
		country_exists = USA
		country_exists = ENG
	}
	abort = {
		OR = {
			NOT = { 
			country_exists = ENG 
			country_exists = USA 
			
			}
			has_war_with = USA
			has_war_with = ENG
		}
	}
	ai_strategy = {
		type = alliance
		id = "ENG"
		id = "USA"
		value = 200
	}
}