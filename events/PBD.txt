on_actions = {
	on_liberate = {
		effect = {
			#If Poland is liberated, PBD is now just Belarus and cannot have cores of it
			if = {
				limit = {
					original_tag = POL
				}
				every_core_state = {
					remove_core_of = PBD
				}
			}
			
		}
	}
}