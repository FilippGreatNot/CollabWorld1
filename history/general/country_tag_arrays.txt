#Probably looks weird and is not optimal but it's more clean looking that way
every_possible_country = {
	limit = {
		is_italian_tag = yes
	}
	add_to_array = { global.italian_tags = THIS }
}
every_possible_country = {
	limit = {
		is_german_tag = yes
	}
	add_to_array = { global.german_tags = THIS }
}
every_possible_country = {
	limit = {
		is_russian_tag = yes
	}
	add_to_array = { global.russian_tags = THIS }
}
every_possible_country = {
	limit = {
		is_chinese_tag = yes
	}
	add_to_array = { global.chinese_tags = THIS }
}